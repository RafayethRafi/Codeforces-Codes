#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,i,sum=0,x,cnt=0;
    cin>>n;
    for(i=0;i<n;i++)
    {
        cin>>x;
        sum+= x;
        if(sum==-1)
            {cnt++;
            sum=0;
            }
    }
    cout<<cnt<<endl;
    return 0;
}
