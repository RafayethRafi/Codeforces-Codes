
#include<bits/stdc++.h>
using namespace std;
int main()
{
    string a,b,s;
    int i;
    cin>>a>>b;
    int x = a.size();
    for(i=0;i<x;i++)
    {
        if(a[i]==b[i])
            s= s+ '0';
        else
            s = s+ '1';
    }
    cout<<s;
    return 0;
}
