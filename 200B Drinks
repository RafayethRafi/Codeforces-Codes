#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin>>n;
    int a[n],i;
    double sum =0;
    for(i=0;i<n;i++)
    {
        cin>>a[i];
        sum = sum +a[i];
    }
    cout<<(sum/(double)n)<<endl;

    return 0;

}
